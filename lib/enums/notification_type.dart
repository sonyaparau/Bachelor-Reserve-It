/*
 * Type of notifications.
 * REQUEST - when a user makes a reservation
 * RESPONSE - when the local accepts/
 *            declines the reservation
 * */
enum NotificationType {
  REQUEST, RESPONSE
}